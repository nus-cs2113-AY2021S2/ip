package duke.exception;

/**
 * Signals an error caused by invalid command entered by the user.
 * Command can be considered invalid for not matching any existing command, wrong formatting,
 * or missing required parameters.
 */
public class InvalidCommandException extends DukeException {
    // nothing to be done
}
