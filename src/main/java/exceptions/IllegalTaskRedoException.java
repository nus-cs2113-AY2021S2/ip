package exceptions;

/**
 * Represents an exception that is thrown when the user tries to mark a task, which was already done, as done again
 */
public class IllegalTaskRedoException extends Exception{
}